# 2026-03-19

## Session 4

operator: Codex

### `/agents` 拆分为 `auto-meta-agent` 子仓

- 本轮将原本直接托管在主仓里的 `agents/` 提取为独立 git 仓，名称定为 `auto-meta-agent`。
- 拆分时保留了原 `agents/` 目录历史，而不是重新复制一份无历史代码。
- 主仓继续把该系统挂载在 `agents/` 路径下，以避免同时改动：
  - `agent.json` 中的 watched-input 路径
  - 现有文档中的运行入口
  - `daily-notes -> doc -> sop` 架构说明
- 与 agent runtime 相关的忽略规则已经从主仓 `.gitignore` 迁移到子仓自身，避免拆分后 runtime 脏文件重新出现在子仓状态里。
- 入口文档和运行架构文档已经补充说明：`agents/` 现在是 `auto-meta-agent` submodule 的挂载点。

### 当前结果

- 主仓保留 `agents/` 作为 submodule path。
- `meta-agent/` 与 `agents/` 现在都以 submodule 方式接入主仓。
- 子仓名已经统一为 `auto-meta-agent`，后续只需要补远端仓并 push 即可完成公开托管。

### 后续注意

- 如果后续创建 `https://github.com/Approaching-AI/auto-meta-agent.git`，需要把当前本地子仓推送过去。
- 在那之前，fresh clone 这个主仓时，`agents/` submodule 还不能从 GitHub 自动初始化。
